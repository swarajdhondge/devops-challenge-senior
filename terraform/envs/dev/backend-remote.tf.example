# Remote state backend. Copy to backend.tf and run: terraform init -backend-config=backend.hcl

terraform {
  backend "s3" {
    # Configuration provided via backend.hcl (generated by scripts/setup.py)
  }
}

